<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="text_header">
        <item name="android:fontFamily">@font/poppins_bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/text_size_header</item>
    </style>

    <style name="text_expand">
        <item name="android:fontFamily">@font/poppins_bold</item>
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="ChipTheme" parent="Base.Widget.MaterialComponents.Chip">

        <!-- Warna teks saat terpilih -->
        <item name="android:checkable">true</item>

        <item name="chipIconVisible">false</item>
        <item name="checkedIconVisible">false</item>
        <item name="closeIconVisible">false</item>

        <item name="checkedIcon">@drawable/ic_mtrl_chip_checked_black</item>

        <item name="chipCornerRadius">12dp</item>
        <item name="chipMinTouchTargetSize">0dp</item>

        <item name="android:textColor">@drawable/custom_color_text_chip</item>

        <item name="chipBackgroundColor">@drawable/custom_bg_chip</item>

    </style>

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">#9588E4</item>
    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- ...
        Tetapkan atribut-atribut lain yang ingin Anda konfigurasi di sini
        ... -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>